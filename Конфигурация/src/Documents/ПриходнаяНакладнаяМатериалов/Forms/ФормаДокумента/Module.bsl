
&НаКлиенте
Процедура МатериалыЦенаПриИзменении(Элемент)
	ПересчитатьСуммуСтроки();
КонецПроцедуры

&НаКлиенте
Процедура МатериалыКоличествоПриИзменении(Элемент)
	ПересчитатьСуммуСтроки();
КонецПроцедуры
&НаКлиенте
Процедура ПересчитатьСуммуСтроки()
	
	// Получаем текущую строку табличной части "Материалы"
	ТекущиеДанные = Элементы.Материалы.ТекущиеДанные;
	
	// Проверяем, что строка существует и не пустая
	Если ТекущиеДанные <> Неопределено Тогда
		
		// Выполняем расчет и записываем результат в реквизит "Сумма"
		ТекущиеДанные.Сумма = ТекущиеДанные.Количество * ТекущиеДанные.Цена;
		
	КонецЕсли;
	
КонецПроцедуры